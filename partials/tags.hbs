<section>
    <h6>Tags</h6>
    {{#get "tags" limit="all"}}
        {{#foreach tags}}
            <a href="{{url}}" class="mdl-button mdl-js-button mdl-button--accent">
                <i class="mdi mdi-tag"></i>  {{name}}
            </a>
        {{/foreach}}
    {{/get}}
</section>
